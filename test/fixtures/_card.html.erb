<div class="card">
  <%= content.yield :image %>
  <div class="card-body">
    <h5 class="card-title"><%= title %></h5>
    <% if content.content_for? :body %>
      <p class="card-text">
        <%= content.content_for :body %>
      </p>
    <% end %>
  </div>
</div>
