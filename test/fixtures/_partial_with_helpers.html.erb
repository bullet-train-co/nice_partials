<%
  partial.helpers do
    def upcase(content)
      content.upcase
    end

    def squish(content)
      content.gsub(/\s+/, " ")
    end

    def upcase_foo
      local_assigns[:foo].upcase
    end
  end
%>

<p><%= partial.upcase yield %></p>
